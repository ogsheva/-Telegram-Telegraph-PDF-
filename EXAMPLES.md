# 📚 Примеры использования

## Примеры Telegraph статей для тестирования

### 1. Простая статья с текстом
```
https://telegra.ph/Test-Article-12-01
```

### 2. Статья с изображениями
```
https://telegra.ph/Photo-Gallery-Example-01-15
```

### 3. Статья с форматированием
```
https://telegra.ph/Rich-Text-Example-02-20
```

## Тестовые сценарии

### ✅ Позитивные тесты

1. **Обычная ссылка**
   ```
   Отправьте: https://telegra.ph/Sample-Article-01-01
   Ожидается: PDF файл со статьей
   ```

2. **Статья с кириллицей**
   ```
   Отправьте: https://telegra.ph/Статья-на-русском-12-25
   Ожидается: PDF с правильной кодировкой
   ```

3. **Длинная статья**
   ```
   Отправьте ссылку на статью >5000 слов
   Ожидается: Многостраничный PDF
   ```

4. **Статья с изображениями**
   ```
   Отправьте ссылку со статьей, содержащей 5+ картинок
   Ожидается: Все изображения в PDF
   ```

### ❌ Негативные тесты

1. **Неверный домен**
   ```
   Отправьте: https://medium.com/some-article
   Ожидается: Сообщение об ошибке с поддерживаемыми доменами
   ```

2. **Несуществующая статья**
   ```
   Отправьте: https://telegra.ph/Non-Existent-Article-99-99
   Ожидается: Сообщение "Не удалось загрузить статью"
   ```

3. **Не URL**
   ```
   Отправьте: просто текст без ссылки
   Ожидается: Сообщение об ошибке валидации
   ```

4. **Пустое сообщение**
   ```
   Отправьте: (пробелы)
   Ожидается: Сообщение об ошибке
   ```

## Примеры команд

### Базовые команды

```
/start
```
**Ответ:**
```
👋 Привет! Я бот для конвертации статей Telegraph в PDF.

📝 Просто отправь мне ссылку на статью с:
• telegra.ph
• teletype.in
• graph.org
...
```

```
/help
```
**Ответ:**
```
📖 Как пользоваться ботом:
1️⃣ Найди статью на Telegraph или Teletype
...
```

### Обработка ссылок

**Пример 1: Успешная конвертация**

```
Пользователь → https://telegra.ph/My-Great-Article-01-15

Бот → ⏳ Загружаю статью...
Бот → 📄 Генерирую PDF...
Бот → 📤 Отправляю файл...
Бот → [Отправляет PDF файл]
      📄 Вот ваша статья в PDF: My Great Article
```

**Пример 2: Ошибка валидации**

```
Пользователь → https://example.com/article

Бот → ❌ Неверная ссылка!

      Пожалуйста, отправь корректную ссылку на статью с:
      • telegra.ph
      • teletype.in
      • graph.org
```

**Пример 3: Статья не найдена**

```
Пользователь → https://telegra.ph/Deleted-Article-01-01

Бот → ⏳ Загружаю статью...
Бот → ❌ Не удалось загрузить статью.
      Проверьте, что ссылка корректна и статья существует.
```

## Производительность

### Ожидаемое время обработки

- **Короткая статья** (< 1000 слов, без изображений): 2-5 секунд
- **Средняя статья** (1000-3000 слов, 2-3 изображения): 5-10 секунд
- **Длинная статья** (> 3000 слов, 5+ изображений): 10-20 секунд

### Ограничения

- **Максимальный размер PDF**: ~50 MB (ограничение Telegram)
- **Максимальное время обработки**: 60 секунд (timeout)
- **Одновременные запросы**: Неограниченно (асинхронная обработка)

## Расширенные примеры

### Статья с различными элементами

**Тест элементов разметки:**

1. Заголовки (H1, H2, H3)
2. Жирный и курсивный текст
3. Списки (маркированные и нумерованные)
4. Цитаты
5. Код (inline и блоки)
6. Ссылки
7. Изображения разных размеров

**Пример Telegraph статьи:**
```html
# Заголовок статьи

## Подзаголовок

Это **жирный текст** и это *курсив*.

### Список покупок:
- Молоко
- Хлеб
- Яйца

> Это цитата известного человека

Код: `print("Hello World")`

[Ссылка на сайт](https://example.com)

![Картинка](https://example.com/image.jpg)
```

**Ожидаемый результат в PDF:**
- Все заголовки сохранены с правильной иерархией
- Форматирование текста сохранено
- Списки корректно отформатированы
- Цитата выделена (border-left)
- Код с монопространным шрифтом
- Ссылки активны (синие, кликабельные)
- Изображение встроено

## Отладка

### Включение подробного логирования

Измените в `main.py`:

```python
logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.DEBUG  # Было: logging.INFO
)
```

### Проверка генерации PDF локально

Добавьте тестовую функцию в `main.py`:

```python
def test_pdf_generation():
    """Тестирование генерации PDF"""
    test_article = {
        'title': 'Тестовая статья',
        'author': 'Тестовый автор',
        'content': '<p>Это тестовый контент статьи.</p><img src="https://via.placeholder.com/150">'
    }
    
    output_path = Path('./test_output.pdf')
    success = generate_pdf(test_article, output_path)
    
    if success:
        print(f"✅ PDF создан: {output_path}")
    else:
        print("❌ Ошибка создания PDF")

if __name__ == '__main__':
    # Раскомментируйте для теста:
    # test_pdf_generation()
    main()
```

### Тестирование парсинга

```python
def test_parsing():
    """Тестирование парсинга Telegraph"""
    test_url = 'https://telegra.ph/Test-Article-01-01'
    article = fetch_article(test_url)
    
    if article:
        print(f"✅ Заголовок: {article['title']}")
        print(f"✅ Автор: {article['author']}")
        print(f"✅ Длина контента: {len(article['content'])} символов")
    else:
        print("❌ Не удалось загрузить статью")
```

## Часто задаваемые вопросы (FAQ)

**Q: Бот не сохраняет изображения в PDF**
A: Проверьте интернет соединение. WeasyPrint загружает изображения онлайн.

**Q: PDF выглядит некрасиво**
A: Отредактируйте CSS в константе `PDF_STYLES` в файле `main.py`

**Q: Можно ли изменить шрифт?**
A: Да, измените `font-family` в `PDF_STYLES`

**Q: Поддерживается ли Teletype?**
A: Да, домен `teletype.in` поддерживается

**Q: Как увеличить лимит времени обработки?**
A: Добавьте `timeout` параметр в `requests.get()`:
```python
response = requests.get(url, headers=headers, timeout=30)  # 30 секунд
```

## Метрики и мониторинг

### Логируемые события

```
INFO - Бот запущен и готов к работе!
INFO - PDF успешно создан: /path/to/file.pdf
INFO - Успешно обработан запрос от пользователя 123456789
ERROR - Ошибка при загрузке статьи: ...
ERROR - Ошибка при парсинге статьи: ...
ERROR - Ошибка при генерации PDF: ...
```

### Рекомендуемые метрики для продакшена

1. **Количество обработанных статей** (счетчик)
2. **Среднее время обработки** (гистограмма)
3. **Процент успешных конвертаций** (gauge)
4. **Размер генерируемых PDF** (гистограмма)
5. **Количество ошибок по типам** (счетчик)

---

**Готовы тестировать! 🧪**
