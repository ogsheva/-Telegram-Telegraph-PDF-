# 🚀 Быстрый старт

Краткая инструкция для максимально быстрого запуска бота.

## Для Linux (Ubuntu/Debian)

```bash
# 1. Установите системные зависимости
sudo apt-get update
sudo apt-get install -y python3-pip python3-venv libpango-1.0-0 libpangoft2-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info

# 2. Создайте и активируйте виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# 3. Установите Python пакеты
pip install -r requirements.txt

# 4. Установите токен бота
export TELEGRAM_BOT_TOKEN="ваш_токен_от_BotFather"

# 5. Запустите бота
python main.py
```

## Для macOS

```bash
# 1. Установите Homebrew (если нет)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 2. Установите зависимости
brew install python3 pango gdk-pixbuf libffi

# 3. Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# 4. Установите пакеты
pip install -r requirements.txt

# 5. Установите токен
export TELEGRAM_BOT_TOKEN="ваш_токен"

# 6. Запустите
python main.py
```

## Для Windows

```powershell
# 1. Скачайте и установите GTK3 Runtime:
# https://github.com/tschoonj/GTK-for-Windows-Runtime-Environment-Installer/releases

# 2. Перезагрузите PowerShell

# 3. Создайте виртуальное окружение
python -m venv venv
venv\Scripts\activate

# 4. Установите пакеты
pip install -r requirements.txt

# 5. Установите токен
$env:TELEGRAM_BOT_TOKEN="ваш_токен"

# 6. Запустите
python main.py
```

## 📱 Как получить токен бота?

1. Откройте Telegram
2. Найдите бота [@BotFather](https://t.me/botfather)
3. Отправьте команду `/newbot`
4. Следуйте инструкциям:
   - Придумайте имя бота (например: "My PDF Bot")
   - Придумайте username бота (должен заканчиваться на "bot", например: "my_pdf_bot")
5. Скопируйте токен, который выдаст BotFather
6. Используйте этот токен в команде выше

## ✅ Проверка работы

После запуска вы должны увидеть:

```
INFO - Бот запущен и готов к работе!
```

Теперь откройте вашего бота в Telegram и отправьте `/start`!

## 🆘 Проблемы?

Смотрите полную инструкцию в [README.md](README.md) в разделе "Решение проблем".
